import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors - Asosiy ranglar
  static const Color primaryBlack = Color(0xFF000000);        // Uber asosiy qora rang
  static const Color primaryWhite = Color(0xFFFFFFFF);        // Toza oq rang
  static const Color uberGreen = Color(0xFF09B83E);           // Uber yashil rang (UberEats)
  
  // Background Colors - Fon ranglari
  static const Color backgroundColor = Color(0xFFF7F7F7);     // Asosiy fon rangi
  static const Color cardBackground = Color(0xFFFFFFFF);      // Karta fon rangi
  static const Color lightGray = Color(0xFFF5F5F5);          // Och kulrang
  static const Color darkBackground = Color(0xFF1C1C1E);      // Tungi rejim uchun
  
  // Text Colors - Matn ranglari
  static const Color primaryText = Color(0xFF000000);        // Asosiy matn rangi
  static const Color secondaryText = Color(0xFF8E8E93);      // Ikkinchi darajali matn
  static const Color lightText = Color(0xFFFFFFFF);          // Oq matn
  static const Color hintText = Color(0xFFC7C7CC);           // Placeholder matn
  static const Color disabledText = Color(0xFFAEAEB2);       // O'chirilgan matn
  
  // Button Colors - Tugma ranglari
  static const Color primaryButton = Color(0xFF000000);       // Asosiy tugma (qora)
  static const Color secondaryButton = Color(0xFFE5E5EA);     // Ikkinchi tugma (kulrang)
  static const Color buttonText = Color(0xFFFFFFFF);          // Tugma matni (oq)
  static const Color secondaryButtonText = Color(0xFF000000); // Ikkinchi tugma matni
  static const Color disabledButton = Color(0xFFE5E5EA);      // O'chirilgan tugma
  
  // Input Field Colors - Kiritish maydonlari
  static const Color inputBackground = Color(0xFFF2F2F7);     // Input fon rangi
  static const Color inputBorder = Color(0xFFE5E5EA);        // Input chegara rangi
  static const Color focusedBorder = Color(0xFF000000);      // Faol input chegarasi
  static const Color inputText = Color(0xFF000000);          // Input matn rangi
  
  // Status Colors - Holat ranglari
  static const Color successGreen = Color(0xFF34C759);       // Muvaffaqiyat rangi
  static const Color errorRed = Color(0xFFFF3B30);           // Xato rangi
  static const Color warningOrange = Color(0xFFFF9500);      // Ogohlantirish rangi
  static const Color infoBlue = Color(0xFF007AFF);           // Ma'lumot rangi
  
  // Map Colors - Xarita ranglari
  static const Color mapBackground = Color(0xFFF0F0F0);      // Xarita fon rangi
  static const Color routeColor = Color(0xFF000000);         // Marshrut rangi
  static const Color locationPin = Color(0xFF000000);        // Joylashuv belgisi
  static const Color destinationPin = Color(0xFF09B83E);     // Manzil belgisi
  
  // Divider & Border Colors - Ajratuvchi va chegara ranglari
  static const Color dividerColor = Color(0xFFE5E5EA);       // Ajratuvchi chiziq
  static const Color borderColor = Color(0xFFE5E5EA);        // Chegara rangi
  static const Color separatorColor = Color(0xFFC6C6C8);     // Ajratuvchi rang
  
  // Shadow Colors - Soya ranglari
  static const Color shadowColor = Color(0x1A000000);        // Yengil soya
  static const Color cardShadow = Color(0x0F000000);         // Karta soyasi
  
  // Rating & Stars - Reyting va yulduzchalar
  static const Color starYellow = Color(0xFFFFCC02);         // Yulduzcha rangi
  static const Color ratingBackground = Color(0xFFF2F2F7);   // Reyting fon rangi
  
  // Vehicle Type Colors - Transport turlari ranglari
  static const Color economyColor = Color(0xFF8E8E93);       // Economy rang
  static const Color comfortColor = Color(0xFF000000);       // Comfort rang
  static const Color premiumColor = Color(0xFF1C1C1E);       // Premium rang
  
  // Price Colors - Narx ranglari
  static const Color priceText = Color(0xFF000000);          // Narx matni
  static const Color discountPrice = Color(0xFF34C759);      // Chegirma narxi
  static const Color originalPrice = Color(0xFF8E8E93);      // Asl narx (chizilgan)
  
  // Navigation Colors - Navigatsiya ranglari
  static const Color activeTab = Color(0xFF000000);          // Faol tab
  static const Color inactiveTab = Color(0xFF8E8E93);        // Nofaol tab
  static const Color tabBackground = Color(0xFFFFFFFF);      // Tab fon rangi
  
  // Loading & Progress - Yuklanish va jarayon
  static const Color loadingColor = Color(0xFF8E8E93);       // Yuklanish rangi
  static const Color progressBackground = Color(0xFFE5E5EA); // Progress fon
  static const Color progressFill = Color(0xFF000000);       // Progress to'ldirish
  
  // Chat & Messages - Chat va xabarlar
  static const Color userMessage = Color(0xFF000000);        // Foydalanuvchi xabari
  static const Color driverMessage = Color(0xFFF2F2F7);      // Haydovchi xabari
  static const Color messageText = Color(0xFFFFFFFF);        // Xabar matni
  static const Color timeStamp = Color(0xFF8E8E93);          // Vaqt belgisi
}